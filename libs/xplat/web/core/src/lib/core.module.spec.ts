describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { TeamDevMonorepoCoreModule } from './core.module';

describe('TeamDevMonorepoCoreModule', () => {
  it('should work', () => {
    expect(new TeamDevMonorepoCoreModule(null)).toBeDefined();
  });
});
*/
